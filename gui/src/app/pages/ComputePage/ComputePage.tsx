import {
  FunctionComponent
} from "react";

type ComputePageProps = {
  width: number;
  height: number;
};

const ComputePage: FunctionComponent<ComputePageProps> = ({
  width,
  height,
}) => {
  return (
    <div>Test</div>
  )
};

export default ComputePage;