import { FunctionComponent, useEffect } from "react";

type NotebookPageProps = {
  width: number;
  height: number;
};

const NotebookPage: FunctionComponent<NotebookPageProps> = ({
  width,
  height,
}) => {
  return <div>Notebook</div>;
};

export default NotebookPage;
