import { FunctionComponent } from "react"

type Props = {
    width: number
    height: number
    path: string
    condensed?: boolean
}

const TwoPhotonSeriesItemView: FunctionComponent<Props> = ({width, height, path, condensed}) => {
    return <div>TwoPhotonSeriesItemView WIP</div>
}

export default TwoPhotonSeriesItemView